import GlassBoxCanvas from './glassbox/GlassBoxCanvas.js';
import './styles.css';

/** Glassbox route — styles scoped via `.glassbox-shell` inside the canvas component. */
export default function DemoApp() {
  return <GlassBoxCanvas />;
}
